<%= link_to "EMITOSnet", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
      <li>
        <%= link_to t('actions.logout'), destroy_user_session_path, :method => 'delete' %>
      </li>
  <% else %>
      <li>
        <%= link_to 'Login', new_user_session_path %>
      </li>
  <% end %>
  <% if user_signed_in? %>
      <!--
    <li>
     <%= link_to 'Edit account', edit_user_registration_path %>
    </li>
    -->
      <% if current_user.has_role? :admin %>
          <li>
            <%= link_to 'Benutzer', users_path %>
          </li>
      <% end %>
  <% end %>

  <% if user_signed_in? %>
      <% if current_user.has_role?('Admin') || current_user.has_role?('SoundFiles') %>
          <li>
            <%= link_to t('nav.soundfile'), {:action => 'index', :controller => '/soundfile'} %>
          </li>
      <% end %>

      <% if current_user.has_role?('Admin') || current_user.has_role?('CallBox') %>
          <li>
            <%= link_to t('nav.callbox'), {:action => 'index', :controller => '/callbox'} %>
          </li>
      <% end %>

      <% if current_user.has_role?('Admin') || current_user.has_role?('MarketSettings') %>
          <li>
            <%= link_to t('nav.market_setting'), {:action => 'index', :controller => '/market_setting'} %>
          </li>
      <% end %>

      <% if current_user.has_role?('Admin') || current_user.has_role?('MarketCalls') %>
          <li>
            <%= link_to t('nav.marketing_calls'), {:action => 'index', :controller => '/marketing_call'} %>
          </li>
      <% end %>

      <% if current_user.has_role?('Admin') || current_user.has_role?('Statistic') %>
          <li>
            <%= link_to t('nav.statistic'), {:action => 'index', :controller => '/statistic'} %>
          </li>
      <% end %>

      <% if current_user.has_role?('Admin') %>
          <li>
            <%= link_to t('System'), {:action => 'index', :controller => '/setting'} %>
          </li>
      <% end %>
  <% end %>
</ul>
